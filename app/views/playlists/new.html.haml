%h1 Creating Playlist

= error_messages_for :playlist

- form_for(:playlist, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
