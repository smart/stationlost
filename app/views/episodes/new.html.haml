%h1 Creating Episode

= error_messages_for :episode

- form_for(:episode, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
